# Trainer base image deps — 컴포넌트 본문이 runtime import 하는 것만.
# torch 는 Dockerfile 에서 CPU index 로 별도 설치 (여기 두면 CUDA wheel 끌어옴).
# evidently / prometheus-client 는 evidently-job 만 씀 → phase 3 에서 별도 image.
pytorch-lightning==2.2.4
mlflow==2.13.0
boto3==1.34.106
minio==7.2.7
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
joblib==1.4.2
# pyarrow 는 명시하지 않는다 — mlflow 2.13 이 pyarrow<16 을 잡아오고, preprocess.to_parquet 가 그걸 사용.
kubernetes==30.1.0
pyyaml==6.0.1
jinja2==3.1.4
