# Trainer base image dependencies — 컴포넌트 본문이 runtime 에 import 하는 것만.
# kfp 는 외부 컴파일러용이라 trainer image 에선 빠진다 (compile-and-register 의 venv 가 담당).
# evidently / prometheus-client 는 evidently-job 이 쓰지만 trainer image 공유 — phase 3 에서 분리 검토.
pytorch-lightning==2.2.4
mlflow==2.13.0
boto3==1.34.106
minio==7.2.7
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
evidently==0.4.27
prometheus-client==0.20.0
kubernetes==30.1.0
pyyaml==6.0.1
jinja2==3.1.4
